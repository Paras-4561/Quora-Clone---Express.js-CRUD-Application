<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Post</title>
    <link rel="stylesheet" href="/style.css"/>
</head>
<body>
    <h1>Quora Posts</h1>
    <% posts.forEach(post => { %>
        <div class="post">
            <h3 class="user">@ <%= post.username %></h3>
            <h3 class="cont"> <%= post.content %></h3>
            <br>
            <a href="http://localhost:8080/posts/<%=post.id%>">See in detail</a>
            <a href="http://localhost:8080/posts/<%=post.id%>/edit">Edit</a>
            <form method="post" action="/posts/<%= post.id %>?_method=DELETE" class="delete-form" style="display:inline;">
                <button type="submit" class="delete-btn">Delete Post</button>
            </form>


        </div>

    <% }) %>
      <br>
      <br>
      <br>
    <a href="http://localhost:8080/post/new" class="create-btn">Create a New Post</a>
    

</body>
</html>